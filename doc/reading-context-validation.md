# 阅读上下文与引用改进验证记录

日期：2026-09-09；安装包验收更新：2026-09-10。保留现有聊天、消息树和文档适配架构。

## 实现范围

- 普通发送、编辑和重试共用请求准备；缓存原文与索引，每轮按问题选择材料。短文全文、长文检索，多文献共享预算，记录提取完整性和本轮覆盖范围。
- 统一估算系统指令、问题、选区、历史、论文和附件的输入开销，预留输出及 10% 安全空间。模型高级设置支持按端点及模型保存上下文上限，未知模型默认 32,768 tokens。
- 证据带文献、版本、原文和可靠位置，回答使用 `[[cite:ID]]`。流式、历史和普通显示共用解析逻辑；代码中的标记不转换，未知 ID 不生成链接。
- 引用快照绑定回答并保存在现有 `context_refs_json` 中。点击及导出前检查来源是否变更；笔记、复制和导出转换成 Zotero 来源链接。
- 更新中英文摘要、要点、方法、局限性默认模板；保留用户覆盖、标签和排序。翻译模板不变。
- 摘要记录已覆盖消息 ID，仅合并新增旧消息，保护最近五轮；切换分支检查祖先路径，失败时降级到预算内最近历史。

## 自动验证

- `npm run test:unit`：565 项通过。
- `npm run build`：通过，包含 TypeScript 检查。
- 变更代码的 Prettier、ESLint 和 `git diff --check`：通过。
- 新增测试覆盖中英文估算、全文预算、问题变化、多文献分配、页信息异常与空白页、引用解析与代码排除、失效来源、EPUB 路径、消息分支持久化、增量摘要及失败降级。

## Zotero 实机验证

使用独立临时配置与资料库、合成 PDF/EPUB 和本机模拟模型接口；未使用个人资料库或外部付费模型。测试脚本、日志和资料位于 Git 忽略目录。

2026-09-10 将生产构建 `AIdea-3.5.2.xpi` 正式安装到独立测试配置，验证插件 `isActive=true`、`temporarilyInstalled=false`，并核对已安装 XPI 与构建产物哈希一致。应用完整重启后验证插件加载和历史恢复。当前验收运行环境为 Windows、Zotero 10.0.2；测试开始时为 10.0.1，重启期间 Zotero 自身完成升级。未替换个人配置中的插件，也未启用资料库同步。

最终安装包：`.scaffold/build/AIdea-3.5.2.xpi`。SHA-256：`D902E2198121C05491A6EE25680B66739E69C1974666290CAD594BA349148B08`。23:20 更新包安装后再次完整重启，已安装文件与构建文件哈希一致。

| 项目                    | 验证结果                                                                                                                                                                       |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PDF 原生提取及页位置    | 通过；兼容实际 PDFWorker 返回的分页符，并以提取页数交叉校验                                                                                                                    |
| 回答流式引用显示        | 通过；模拟响应在引用标记中间分块                                                                                                                                               |
| 点击回答中的 PDF 引用   | 通过；界面显示第 2/3 页及对应 Results 原文                                                                                                                                     |
| 保存 Zotero 测试笔记    | 通过；笔记包含可保留的来源链接                                                                                                                                                 |
| 引用数据库保存与读取    | 通过；完整应用重启后从历史恢复旧回答，点击其 PDF 引用到第 2/3 页                                                                                                               |
| EPUB 原生打开及章节导航 | 通过；通过 `@` 添加 EPUB，回答显示 Chapter Two 引用；关闭阅读器后点击历史引用首次打开，定位到 Chapter Two 并保持在第 2/2 页；已打开阅读器的跳转也通过                          |
| 多论文连续追问          | 通过；添加两篇 PDF，先问方法再问结果，两轮均包含两篇来源；移除一篇后，后续请求仅保留剩余文献的当前证据                                                                         |
| 阅读器侧栏              | 复测通过；PDF 与 EPUB 均能显示完整输入区、发送并显示引用；PDF 阅读器内点击引用到结果页 2/3                                                                                     |
| 编辑与重试              | 通过；EPUB 阅读器内重试生成 2/2 回答版本；编辑问题从 Chapter Two 结果改为 Chapter One 方法，来源随之更新；重启后编辑分支保留，切回原分支恢复 Chapter Two 引用并跳转到第 2/2 页 |
| 长对话增量摘要          | 通过真实发送链路；导入 20 条合成历史消息，首轮压缩前 10 条，第二轮只增加到前 12 条，最近五轮保留原消息；数据库中各轮快照分别保存 10、12 个已覆盖消息 ID                        |
| 摘要恢复与失败降级      | 通过；完整重启后日志确认从数据库恢复摘要；模拟摘要接口返回 HTTP 400，日志记录降级，正常流式回答仍完成并回到 Ready，估算输入在预算内                                            |

本轮实机发现并修复：

- `@` 文献搜索原先只接受 PDF，改为使用文档适配层识别 PDF/EPUB，并修正 EPUB 附件标签。
- 窄侧栏头部遮挡历史和设置入口，改为在窄容器中将标签导航放到第二行；重启后实机验证按钮完整显示。
- EPUB 初始化分页会覆盖首次章节跳转，现等待阅读器及文档初始化，并在初始布局恢复后再次应用目标位置。已增加等待顺序和挂起标签页的测试。该兼容处理使用 Zotero 内部初始化 Promise 和 300 ms 布局等待，目前仅在上述版本及合成 EPUB 中实机验证，其他版本及复杂书籍仍需回归。

2026-09-17 EPUB 导航补测：修复 Zotero 10.0.2 的分页边界取整问题（原生目录也可复现）。正常定位后检查目标是否确实在相邻下一页，仅此时做一次原生翻页；短书的原生末页判断同样取整且拒绝翻页时，复用原生位置、锚点和状态更新方法。元素边界 CFI 先解析为其首个文字位置，原始保存的引用不变。兼容层检测所需接口，缺少接口时保留原生行为，不修改书籍内容或阅读模式。新增 7 项回归测试，全套 594 项通过。两份合成 EPUB 的 href/CFI 四种导航路径通过原生 DOM 可见坐标断言；Chapter Two 标题和正文已可视确认。最终冷启动后的可视复测因桌面锁定未执行，运行时断言通过。此兼容层使用 Zotero 内部接口，未来 Zotero 版本仍需回归。

23:12—23:25 补测修正了侧栏的初始化可见性判断：Zotero 在连接自定义面板时尚未提供 `tabType` 和 `item`，此时不再直接禁用面板，待上下文确定后再筛选；条目变化回调也使用传入的条目信息。新增未知上下文、阅读器、资料库及不支持标签类型的回归测试。冷启动后在 PDF 与 EPUB 中分别展开 AIdea，等待原生异步渲染后均可正常使用；首次展开仍可能有数秒加载等待。

本轮结论：上述本地功能验收通过，原先列出的侧栏、编辑重试及长历史主流程缺口已补齐。摘要分支失效等边界继续由自动测试覆盖，不代表所有边界组合均逐项操作过界面。未进行真实模型回答质量评估或跨平台、跨 Zotero 版本验收。

## 限制

2026-09-17 22:40 后补齐最终构建的冷启动电脑操作验收：从历史恢复回答后实际点击 PDF 引用，显示第 2/3 页及 Results 正文；返回文库点击 EPUB 引用，显示 Chapter Two 标题和 Results 正文，位置为第 2/2 页。此前锁屏造成的最终可视复测缺口已补齐。媒体恢复和 ZIP 菜单往返结果见 `chat-transfer-validation.md` 的同日补验记录。本轮使用合成文档及独立测试配置，没有覆盖其他 Zotero 版本或复杂 EPUB 排版。

- token 数为轻量估算，图片与原生附件采用预留额度，不等同于服务商精确计数。
- 本次提供可靠页级或章节/已有锚点定位，不做逐字匹配和高亮；EPUB 视口受原生分页影响。
- 没有可靠位置时只打开来源。EPUB 仅有章节路径、没有可携带 CFI 时，导出链接降级为文献来源链接。
- 导出时检查来源版本；已经生成的笔记或外部副本中的链接不会在日后自动更新。
- 引用匹配只验证来源 ID 和位置，不验证模型结论的语义正确性；模拟模型测试不代表真实模型回答质量评估。
- `reference/` 仅作本地参考，保持忽略；本次未发布或推送。
